# Polymarket Bot Configuration

# Your Polygon wallet private key (for signing orders)
PRIVATE_KEY=0x_your_private_key_here

# Strategy settings
STREAK_TRIGGER=4          # Bet after N consecutive same outcomes
BET_AMOUNT=5              # USD per bet (min 5 on Polymarket)
MAX_DAILY_BETS=50         # Max bets per day
MAX_DAILY_LOSS=50         # Stop trading if daily loss exceeds this

# Timing
ENTRY_SECONDS_BEFORE=30   # Enter N seconds before window opens

# Mode
PAPER_TRADE=true          # Set to false for live trading

# Deribit API (for TurtleQuant IV surface)
DERIBIT_CLIENT_ID=your_client_id_here
DERIBIT_CLIENT_SECRET=your_client_secret_here

# TurtleQuant settings
ENTRY_THRESHOLD=0.05
KELLY_FRACTION=0.25
STARTING_NAV=100.0

# Resolution oracle (fill in after verifying resolved BTC/ETH markets)
# RESOLUTION_ORACLE_SOURCE=
# RESOLUTION_ORACLE_TIMESTAMP_UTC=

# Timezone — any IANA name (e.g. UTC, Europe/Paris, America/New_York, Asia/Singapore)
TIMEZONE=UTC

# Data source for OHLCV fetching
# auto    — try Binance; on 451 geo-block fall back to OKX, then Gate.io
# binance — always Binance (use when your server is not geo-blocked)
# bybit   — always Bybit  (globally accessible on most hosts, years of 5m history)
# okx     — always OKX    (EU/France-accessible, full history back to Jan 2022)
# gateio  — always Gate.io (limited: ~34d for 5m, ~104d for 15m)
DATA_SOURCE=auto

# File paths — override these when running in Docker (matches docker-compose.yml volumes)
# TRADES_FILE=/app/state/trades.json
# HISTORY_FILE=/app/state/trade_history_full.json
# LOG_FILE=/app/state/bot.log
