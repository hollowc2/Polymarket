# Stale TurtleQuant State

These JSON files are old repo-local samples, not the active TurtleQuant runtime
state.

Use `/opt/turtlequant/state` for the running shadow bot. If the live compose
override is active, use `/opt/turtlequant/state/live-state`.
